Inital version of ncip v.1 ncip responder for the evergreen / open-ils

you can initiate / test with the following :

curl -v --insecure --data @NCIPmsgs/LookupUser.ncip -X POST 'https://target.host.here/ncip.cgi'